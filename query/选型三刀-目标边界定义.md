---
AIGC:
    ContentProducer: Minimax Agent AI
    ContentPropagator: Minimax Agent AI
    Label: AIGC
    ProduceID: bf4f1ac2b0b112b8a504eb6e8aeb9451
    PropagateID: bf4f1ac2b0b112b8a504eb6e8aeb9451
    ReservedCode1: 3046022100821745f571661e291e10b625a2fef3e62d744fe2b93ecee6a156650cd0149190022100868bb1fcff39543f5e5bd7b7486521e5a1a7d334999b1baa8ef039ed44f31464
    ReservedCode2: 30440220754a03e27fc6381355ff210047bb5ccaebbdfae7141f829c2e0c6eb52475f3bd02200c81432dea00c5af653a91a49585b8e62e6ccdf6bb7b2d90fdd92deb2c730571
---

# 项目思考：选型三刀，目标边界定义

> 时间：2026-03-20
> 人物：黑爪爪 & 用户

---

## 用户问题

Moonshine AI 二次开发方案已出，Electron + moonshine-small，选型方向确认。

**核心问题：这个项目到底要「做到什么程度才算够」？**

---

## 黑爪爪分析

### 第一刀：质疑根

「Electron + moonshine-small」，不是技术选型，是**立场宣言**。

> 选 Electron，是因为愿意用一点运行时开销换一套代码跨三端。
> 选 small，是因为体积和速度的乘积最优。

这是对的。技术选型没有绝对优劣，只有「此刻，这个团队，这个目标」的最优解。

### 第二刀：方案缺了什么

多了「怎么做」，少了一层：

> **做到什么程度才算够？**

### 第三刀：三个必须现在定义的边界

**边界一：推理延迟目标**

moonshine-small 官方延迟 <100ms，但这只是模型推理。

端到端链路：
```
麦克风采集（0.3s buffer）→ 传输 → moonshine（~80ms）→ 输出
= 端到端 380ms
```

0.3s 的 chunk 如果砍到 0.16s，延迟可降至 ~240ms，但推理线程压力翻倍。

→ **核心问题：延迟目标定在 500ms 可接受，还是必须 <300ms？**

**边界二：精度 vs 隐私**

medium 比 small 精度高 10-15%，但体积从 95MB → 245MB，安装包 120MB → 300MB。

→ **核心问题：用户愿不愿意多下 180MB 换 10-15% 精度提升？**

**边界三：说话人分离的代价**

pyannote.audio 需要额外 ~200MB 模型，且需要多声道物理隔离才能真正分离两个人。单声道做声纹分类，那叫「猜」，不叫「分离」。

→ **核心问题：双路分离是物理隔离的双人场景，还是会议场景？**

### 结论

在继续开发之前，需要先确认第一版交付标准：

| 目标 | 时间 |
|------|------|
| 「能跑起来，录一段，导出文字」 | 一个周末 |
| 「产品级 UI，打包发布，能给朋友用」 | 2-4 周 |

---

## 待确认

- [ ] 延迟目标：500ms 还是 <300ms？
- [ ] 模型选择：small（95MB）还是 medium（245MB）？
- [ ] 说话人分离：是物理隔离双人场景，还是会议多人场景？
- [ ] 第一版交付标准：Demo 可跑，还是产品级发布？
